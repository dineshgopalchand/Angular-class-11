export class AppError {
    constructor(public originalError?: any) {
        if (originalError) {
            // call some function/services to store or create log on server
            // return originalError;
            // if(originalError i)
        }

    }
}
